module.exports = {
    buildReport: function(type, getsModule) {
        var finalReport = {};
        if (type === 'rcq') {
            finalReport.title = 'Readiness to Change'
        }
        
        
        
        return finalReport;
    }
}